<div class="container">
  <div class="row">
    <div class="col-md-5 offset-md-1">
      <p>ジャンル編集</p>
    </div>
  </div>

    <div class="col-md-5">
      <%= form_with model: @genre, url: admin_genre_path(@genre) do |f| %>
      <div>
        ジャンル名
        <%= f.text_field :name %>
        <%= f.submit '変更を保存' %>
      </div>
      <% end %>
    </div>
</div>
