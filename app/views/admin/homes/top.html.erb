<br>
<br>
<div class="container">
  <div class="row">
    <div class="col-md-10">
      <h1>注文履歴一覧</h1>
      <table class="table">
        <thead>
          <p>
          <th>購入日時</th>
          <th>購入者</th>
          <th>注文個数</th>
          <th>注文ステータス</th>
          </p>
        </thead>

        <tbody>
          <% @orders.each do |order| %>
          <tr>
            <td>
              <%= link_to order.updated_at.strftime('%Y/%m/%d %H:%M:%S'), admin_order_path(order.id) %>

            </td>
            <td>
              <%= order.customer.full_name %>
            </td>
            <td>
              <% @amount=0 %>
              <% order.order_details.each do |orders_detail| %>
              <% @amount+=orders_detail.amount %>
              <% end %>
              <%= @amount %>
            </td>
            <td>
              <%= order.status_i18n %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
